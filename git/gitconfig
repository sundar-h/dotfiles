[url "https://gitlab"]
	insteadOf = git://gitlab
[user]
	name = sanghaifa
	email = sanghaifa@sensetime.com
[core]
	editor = code --wait
[diff]
	tool = vscode
[difftool "vscode"]
	cmd = code --wait --diff $LOCAL $REMOTE
[merge]
	tool = vscode
[mergetool "vscode"]
	cmd = code --wait $MERGED
[pull]
	ff = only
